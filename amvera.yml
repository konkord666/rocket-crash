meta:
  environment: python
  toolchain:
    version: 3.11

build:
  command: pip install -r requirements.txt

run:
  command: python server.py
  port: 8080

env:
  - BOT_TOKEN
  - PROVIDER_TOKEN
  - PORT=8080
